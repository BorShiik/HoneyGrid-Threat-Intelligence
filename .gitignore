# ── CI/CD wyłączone — token OAuth bez scope `workflow` ─
# Pliki GitHub Actions celowo poza repo; testy uruchamiamy lokalnie
.github/workflows/

# ── .NET ─────────────────────────────────────────────
bin/
obj/
out/
publish/
src/HoneyGrid.Functions/func.zip
*.user
*.suo
.vs/
artifacts/
TestResults/
*.binlog
project.lock.json
*.nupkg
!packages/**/*.nupkg

# ── Node / frontend (Vite + React) ───────────────────
node_modules/
dist/
dist-ssr/
.vite/
coverage/
*.tsbuildinfo
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Środowisko / sekrety ──────────────────────────────
# (pliki *.bicepparam trzymamy w repo — to parametry, nie sekrety)
.env
.env.*
!.env.example
local.settings.json
*.pfx
*.key
secrets.json

# ── Azure ─────────────────────────────────────────────
.azure/
.azurite/
__azurite_db*__.json
__blobstorage__/
__queuestorage__/

# ── macOS ─────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── IDE ───────────────────────────────────────────────
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ── Logi i pliki tymczasowe ───────────────────────────
*.log
logs/
*.tmp
*.bak
